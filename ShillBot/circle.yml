machine:
  python:
    version: 3.6.0

test:

  override:

    - python -m unittest discover
    - py.test seng299/ShillBot/tests
    
    ' ' ' 
    test:
    override:
        - py.test <optional path to subdir with tests>
        ' ' '

