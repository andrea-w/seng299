machine:
  python:
    version: 3.6.0

test:

  override:

    - python -m unittest discover
    
    ' ' ' 
    test:
    override:
        - py.test <optional path to subdir with tests>
        ' ' '

dependencies:
    pre:
       -  pip install pytest
